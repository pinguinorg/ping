// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "pinguinorg.ping"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
